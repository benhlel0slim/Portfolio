export type DictionaryProps = {
  navbar: {
    about: string;
    project: string;
    tech: string;
  };
  landingPage: {
    name: string;
    title: string;
    description: string;
  };
  about: {
    tag: string;
    description: string;
  };
  projects: {
    tag: string;
  };
  footer: {
    description: string;
    link: string;
  };
};
